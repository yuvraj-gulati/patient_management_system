import { FaxNumberFormatPipe } from './fax-number-format.pipe';

describe('FaxNumberFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FaxNumberFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
